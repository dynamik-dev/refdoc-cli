This is a markdown file with no headings at all.

It just has regular paragraphs of text that should be treated as a single chunk.

The chunker needs to handle this gracefully, using the filename as the title.

Here is another paragraph with some **bold** and *italic* text to make sure formatting is preserved in the body.
